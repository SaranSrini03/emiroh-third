import influencers from "@/data/influencer";
import { chartData } from "@/data/chartData";
import { allStatsSets } from "./statsData";
import { data as initialData } from "@/lib/imports";
import trafficData from "@/data/trafficData";   

// Table data (from imports)
export const tableData = initialData;

export { influencers, chartData, allStatsSets, trafficData };
